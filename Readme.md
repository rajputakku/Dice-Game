The package contains the code for dice game assignment. It is written in Ruby.

### Setup
Install dependencies using `bundle install`

### Start program
Run `ruby solution.rb`

### Unit tests
Run `bin/rspec --format doc`